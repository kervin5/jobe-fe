import React from "react";
import SocialMedia from "./SocialMedia";

const TrackableSocialMediaLinks = ({ url }) => {
  return <SocialMedia url={url} />;
};

export default TrackableSocialMediaLinks;
